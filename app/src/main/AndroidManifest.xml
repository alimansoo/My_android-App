<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.danesh_yar"
    android:versionCode="101"
    android:versionName="3.1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="دانش یار"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".test" />
        <activity android:name=".AddLesson" />
        <activity android:name=".Sleep" />
        <activity android:name=".Win_finish" />
        <activity android:name=".SetLesson" />
        <activity android:name=".Info" />
        <activity android:name=".Win_Program" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".Services.NotificationActionService"/>
        <service android:name=".Services.onClearFromRecivedService"/>
    </application>

</manifest>